"""Getting a page: over HTTP, or through a real browser when the data is not in
the HTML that arrives over the wire.

Both implementations satisfy :class:`catalog_scraper.fetch.base.Fetcher`, so
switching a source between them is a configuration change.
"""
